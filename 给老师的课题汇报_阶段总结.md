# 给老师的课题汇报（阶段总结）

## 一、课题名称

基于改进 YOLOv8 的焊缝缺陷检测方法研究与实现

---

## 二、课题背景与研究目标

本课题面向工业焊缝缺陷检测场景，目标是构建一个既具有研究价值、又具备实际使用能力的焊缝缺陷识别系统。

焊缝缺陷检测任务具有以下特点：

- 缺陷目标普遍较小，例如裂纹、气孔、飞溅等都属于典型小目标。
- 工业图像背景复杂，焊缝表面常伴随反光、噪声和纹理干扰。
- 不同类别样本数量不均衡，容易造成训练偏置。
- 数据集中存在不完全标准化标注，直接训练会遇到兼容性问题。

围绕以上问题，本课题希望完成以下目标：

1. 完成焊缝缺陷数据集的整理与可训练化处理。
2. 以 YOLOv8 为基线构建焊缝缺陷检测模型。
3. 从预处理、特征融合、注意力机制和损失函数四个方面进行改进优化。
4. 将改进方法真正接入实际训练链路，而不是停留在理论或结构设计层面。
5. 最终实现命令行和图形界面两种使用方式，使模型能够对焊缝图像输出缺陷识别结果和文字分析报告。

---

## 三、前期工作与总体技术路线

本课题的整体技术路线可以概括为：

1. 数据集分析与问题排查。
2. 基于 Ultralytics/YOLOv8 的基线方案搭建。
3. 引入传统机器视觉前端增强方法，提高焊缝细节可见性。
4. 设计轻量注意力和改进特征融合结构，增强模型对小缺陷的表达能力。
5. 设计并接入 Focal-EIoU 风格损失函数，提升类别不平衡场景下的分类和定位效果。
6. 完成训练、评估、推理、可视化和 GUI 使用链路。

目前已经从“只有数据集和需求说明”推进到“具备完整代码框架、训练链路和可用界面”的阶段。

---

## 四、目前已经完成的主要工作

## 4.1 完成项目工程化搭建

已将原始工作区扩展为完整的研究型项目，主要包括：

- 配置目录 `configs/`
- 训练、推理、评估脚本目录 `scripts/`
- 核心源码目录 `src/weld_project/`
- 训练输出目录 `runs/`
- 推理输出目录 `outputs/`

目前项目已具备数据分析、模型训练、模型评估、图片识别、报告输出和 GUI 使用等能力。

## 4.2 完成数据集清洗与兼容处理

在实际处理数据集时发现，原始数据并非纯标准 YOLO 检测框标注，而是包含大量 polygon 多边形标注。

为此，已完成以下工作：

- 自动识别标签是 bbox 还是 polygon。
- 将 polygon 自动转换为检测框形式。
- 构建训练时使用的镜像检测数据集。
- 自动生成运行时所需的数据集 YAML 配置。

这一步保证了数据集可以稳定接入 Ultralytics 检测训练流程。

## 4.3 完成研究型改进模块实现

已实现以下核心改进模块：

1. 自适应预处理模块
作用：增强图像局部对比度，改善光照不均和边缘弱化问题。

2. 轻量注意力模块
作用：让网络更聚焦于焊缝缺陷区域，抑制复杂背景干扰。

3. 改进特征融合模块
作用：增强多尺度特征融合能力，提高模型对小目标缺陷的敏感性。

4. 自定义损失函数模块
作用：分类项使用 Focal 风格，回归项使用 EIoU 风格，以应对样本不平衡和定位困难问题。

## 4.4 完成改进模块接入实际训练链路

这一部分是本课题目前最关键的进展之一。

已完成的不是简单“写了模块”，而是：

- 自定义结构已经通过模型 YAML 接入 YOLOv8 风格训练链路。
- 自定义损失函数已经通过自定义 criterion 真正替换默认训练损失。
- 已专门编写验证脚本，确认当前训练时实际启用的是 `CustomV8DetectionLoss` 和 `CustomBboxLoss`。

也就是说，损失函数优化已经从“理论设想”变成了“实际生效的训练机制”。

## 4.5 完成推理系统与 GUI

目前已经完成以下使用链路：

- 单张图片识别
- 批量图片识别
- 输出检测结果图
- 输出文字分析报告
- 输出 JSON / CSV / TXT 汇总结果
- Windows 图形界面调用

因此，本项目已经不只是研究代码，也已经具备基础的工程应用能力。

---

## 五、当前实验进展与阶段性结果

## 5.1 数据集基本情况

当前数据集统计结果如下：

- 类别数：4
- 类别名称：Crack、Porosity、Spatters、Welding line
- 训练集图像数：4815
- 验证集图像数：800
- 测试集图像数：805
- 标注特点：polygon 为主，已转换为检测框训练格式
- 训练集 tiny box ratio 约为 0.871

从数据特征可以看出，该任务具有明显的小目标检测特性，因此从特征融合、注意力和损失函数角度进行优化是合理且必要的。

## 5.2 训练进展

前期已完成自定义模型的 1 epoch 冒烟训练，用于验证训练链路可用性。

在此基础上，已经启动并持续推进正式 50 epoch 训练。目前该训练已恢复至断点续训状态，并继续向后训练。

截至目前记录到的关键结果如下：

- 第 33 轮时：
  - mAP50 = 0.60925
  - mAP50-95 = 0.32657

- 第 35 轮时：
  - mAP50 = 0.58797
  - mAP50-95 = 0.31839

从整体趋势看，模型在前 30 轮左右呈持续提升状态，说明当前改进结构与训练策略具有较好的收敛能力。

需要说明的是，第 34 轮后曾因 Windows 平台 OpenMP 运行时冲突导致训练中断，后续已通过显式设置环境变量完成断点恢复，当前训练可以继续推进。

## 5.3 当前阶段性结论

当前结果表明：

1. 训练链路已经跑通，说明模型结构、数据集、损失函数和训练框架之间兼容良好。
2. 改进模型已经取得了较初期明显更高的检测精度。
3. 课题已从“搭框架”阶段进入“精度优化和论文整理”阶段。

---

## 六、目前已解决的关键问题

在课题推进过程中，已解决以下关键技术问题：

## 6.1 数据兼容问题

原始标签为 polygon，多数现成检测训练流程无法直接使用。现已实现自动转换和运行时数据配置生成。

## 6.2 训练环境问题

修复了 `machine` 环境中 PyTorch CUDA 安装异常的问题，并确认训练能够使用 GPU。

## 6.3 自定义模块接入问题

已解决自定义结构在 Ultralytics 中的解析和前向问题，使自定义 YAML 模型能够成功训练和推理。

## 6.4 自定义损失函数接入问题

已将自定义损失真正接入训练 criterion，并通过独立验证脚本确认生效。

## 6.5 训练中断与恢复问题

针对正式训练被中断的情况，已实现从 `last.pt` 断点续训，并处理了 OpenMP 冲突导致的中途退出问题。

## 6.6 非代码用户使用问题

已提供 GUI 图形界面，使用户无需手写命令即可完成单图和批量识别。

---

## 七、本课题当前体现出来的创新点

从目前的实现和研究路线看，本课题的创新点主要集中在以下几个方面：

## 7.1 传统机器视觉与深度学习检测结合

在深度学习检测框架前加入传统图像增强思路，使模型在焊缝复杂光照和反光条件下具有更好的输入质量。

## 7.2 面向焊缝小缺陷的特征融合优化

通过轻量化改进特征融合模块增强浅层细节和深层语义的协同，提升对小尺度缺陷目标的表达能力。

## 7.3 面向焊缝复杂背景的注意力增强

通过轻量注意力机制增强缺陷区域响应，抑制焊缝纹理、反光和背景噪声造成的误检。

## 7.4 将 Focal-EIoU 风格损失真正嵌入训练链路

这一点相对更具有研究意义。很多工作停留在“提出一个损失函数”，但并未真正完成工程落地。本课题已经把该损失接入实际训练流程，并用实际训练验证了其可运行性。

---

## 八、存在的问题与下一步计划

尽管当前进展顺利，但仍有以下工作需要继续推进：

## 8.1 需要完成 50 epoch 正式训练并整理最终指标

训练结束后，需要系统整理：

- 最优权重文件
- 最佳 epoch
- mAP50 与 mAP50-95
- precision 与 recall
- confusion matrix、PR 曲线等图表

## 8.2 需要继续做消融实验和对比实验

后续建议按以下思路继续：

1. 基线模型
2. 基线 + 预处理
3. 基线 + 预处理 + 改进特征融合
4. 基线 + 预处理 + 改进特征融合 + 注意力
5. 基线 + 全部模块 + 改进损失函数

通过消融实验验证每一项改进对最终性能的贡献。

## 8.3 需要进一步凝练论文表述

接下来需要把当前代码实现转化为论文中的：

- 方法章节
- 公式表述
- 实验设计
- 创新点归纳
- 图表和结果分析

相关材料已经开始整理，包括方法章节公式模板等文档。

---

## 九、当前阶段总结

截至目前，本课题已完成从数据集整理、工程框架搭建、改进模块实现、损失函数接入、训练链路打通，到推理系统与 GUI 构建的一整套工作。

目前课题已经不再处于“前期准备”阶段，而是已经进入“训练优化、实验验证和论文整理”的中期阶段。

总体来看，当前工作达到了以下几点：

1. 研究路线明确。
2. 工程框架完整。
3. 改进点已经落地为真实代码。
4. 自定义损失函数已经真正进入训练链路。
5. 正式训练已取得阶段性结果。
6. 系统已经具备一定的实际使用能力。

因此，课题目前的重点将从“能不能做出来”逐步转向“如何继续提升性能”和“如何更规范地形成论文成果”。

---

## 十、拟向老师汇报的简短口头总结

如果需要当面简要汇报，可以概括为：

本阶段已经完成了焊缝缺陷检测课题的整体工程搭建，解决了数据集 polygon 标注兼容、YOLOv8 自定义结构接入、自定义损失函数接入、训练环境修复和 GUI 推理等关键问题。目前模型正式训练已经推进到 35 轮左右，阶段性最优结果达到 mAP50 约 0.61、mAP50-95 约 0.33。下一步将继续完成正式训练，并开展消融实验、结果整理和论文撰写工作，重点突出焊缝缺陷场景下预处理、特征融合、注意力机制和改进损失函数的综合优化效果。
