# 焊缝缺陷检测项目学习说明（新手版）

## 1. 这份文档是干什么的

这份文档是给当前项目的使用者和学习者准备的。

目标有 4 个：

1. 说明我在这个项目里具体做了什么。
2. 说明这个项目有哪些优化点和创新点。
3. 告诉你这些代码现在怎么用。
4. 告诉你如果你后续自己学习和继续优化，应该从哪里下手。

这个项目目前已经具备以下能力：

- 可以对焊缝缺陷数据集进行统计分析。
- 可以训练 YOLOv8 风格的自定义焊缝缺陷检测模型。
- 可以输出训练结果、推理结果图和文字分析报告。
- 可以通过命令行或者图形界面输入一张焊缝照片，输出识别结果。

---

## 2. 我在这个项目中做了什么

### 2.1 搭建了完整的项目结构

我把原本只有数据集和需求说明的工作区，扩展成了一个完整的研究型项目。

核心目录如下：

- `configs/`：配置文件
- `scripts/`：训练、推理、分析、GUI 启动脚本
- `src/weld_project/`：核心源码
- `runs/`：训练输出
- `outputs/`：推理结果输出
- `.vscode/`：VS Code 工作区配置

### 2.2 处理了数据集实际问题

你的 `1` 目录中的数据集并不是完全标准的 5 列 YOLO 检测框标注，而是大量多边形标注。

我做了以下处理：

- 自动识别标签是 bbox 还是 polygon。
- 将 polygon 标签自动转换成检测框形式。
- 自动生成训练时可用的检测数据镜像目录。
- 生成运行时可用的 `dataset_runtime.yaml`。

这部分代码在：

- `src/weld_project/data/dataset_utils.py`

### 2.3 实现了研究型改进模块

我没有只停留在“提建议”层面，而是把改进模块真正写成了可复用代码。

已经实现的模块包括：

1. 自适应预处理模块
   - 文件：`src/weld_project/models/modules/adaptive_preprocess.py`
   - 作用：改善光照不均、提升局部细节表达。

2. 轻量注意力模块
   - 文件：`src/weld_project/models/modules/attention.py`
   - 当前支持：EMA、SimAM 风格。
   - 作用：让模型更聚焦焊缝缺陷区域。

3. 增强 FPN 模块
   - 文件：`src/weld_project/models/modules/enhanced_fpn.py`
   - 作用：强化多尺度特征融合，提升小目标检测能力。

4. 自定义损失函数基础模块
   - 文件：`src/weld_project/models/modules/losses.py`
   - 包含：Focal Loss、EIoU 风格损失。

### 2.4 把自定义结构真正接进了 Ultralytics 训练链路

这一点非常重要。

很多项目只是单独写了模块，但并没有真正让训练框架用到它们。
我这里已经把自定义模块和自定义损失接入到了 Ultralytics/YOLOv8 风格链路中。

接入文件在：

- `src/weld_project/integrations/ultralytics_custom.py`
- `src/weld_project/integrations/ultralytics_loss.py`
- `configs/models/yolov8_weld_custom.yaml`

当前的机制是：

- 自定义模型结构通过 YAML 解析进入 YOLO。
- 当 `loss_type` 设为 `focal_eiou` 时，会启用自定义检测损失。
- 自定义损失中，分类项使用 Focal 风格，框回归使用 EIoU 风格。

### 2.5 做了训练、推理、GUI 和结果报告

我补齐了完整使用链路：

1. 数据分析脚本
   - `scripts/analyze_dataset.py`

2. 通用训练脚本
   - `scripts/train.py`

3. 自定义 YOLO 训练脚本
   - `scripts/train_custom_yolo.py`

4. 命令行推理脚本
   - `scripts/predict_report.py`

5. 图形界面推理脚本
   - `scripts/weld_gui.py`

6. Windows 双击启动脚本
   - `launch_gui.bat`
   - `train_formal_50epochs.bat`
   - `train_formal_100epochs.bat`

### 2.6 修复了运行环境和 VS Code 问题

我还额外处理了这些问题：

- 修复了 `machine` 环境中损坏的 PyTorch CUDA 安装。
- 确认训练确实使用了 GPU。
- 解决了 VS Code 中脚本导入报错的问题。
- 增加了 `.vscode/settings.json`、`pyrightconfig.json` 和 `src/__init__.py` 来解决工作区导入识别问题。

---

## 3. 这个项目有哪些优化点和创新点

如果从“课题研究”或“毕业设计优化”角度看，目前项目里的优化点主要有 4 类。

### 3.1 传统机器视觉 + 深度学习结合

这不是纯粹只跑一个 YOLO。

在推理与研究设计中，加入了传统图像增强思路：

- 灰度世界白平衡
- CLAHE 局部对比度增强
- 双边滤波降噪

代码在：

- `src/weld_project/data/transforms.py`

意义：

- 焊缝图像常见光照不均、反光、局部噪声。
- 这种预处理可以作为“传统机器视觉先验”嵌入完整方案中。

### 3.2 更适合小缺陷的特征融合

焊缝裂纹、气孔本质上都偏小目标。

增强 FPN 的意义是：

- 让浅层细节和深层语义信息融合得更充分。
- 缓解普通 YOLO 对小目标不够敏感的问题。

### 3.3 更适合缺陷区域聚焦的注意力机制

注意力机制本身不是新东西，但放在焊缝缺陷场景有很强现实意义。

因为工业背景里：

- 焊缝纹理复杂
- 背景噪声多
- 飞溅与正常纹理容易混淆

所以让模型“少看背景、多看缺陷”是合理优化方向。

### 3.4 更适合焊缝缺陷的损失函数

这一部分是项目中真正带研究价值的优化点之一。

当前已经接入的思路是：

- 分类损失用 Focal 风格，降低简单样本的主导作用。
- 回归损失用 EIoU 风格，更重视框中心和宽高偏差。

为什么适合焊缝缺陷：

- 类别不平衡明显。
- 小目标、难样本较多。
- 普通 BCE 和普通 IoU 往往不足以强调困难样本。

---

## 4. 当前项目已经做到什么程度

截至目前，项目不是停留在“框架空壳”，而是已经跑通了很多关键步骤。

### 4.1 数据分析结果

项目已经分析过你的数据集，关键结论如下：

- 类别数：4
- 训练集图片数：4815
- 验证集图片数：800
- 测试集图片数：805
- 标注模式：polygon 为主
- 训练集 tiny box ratio 约为 0.871

说明：

- 这个数据集以小目标为主。
- 小目标检测优化非常必要。

### 4.2 已完成一轮 1 epoch 冒烟训练

训练输出目录：

- `runs/weld_defect_research/smoke_custom_yolo_bg2/`

已经产出：

- `best.pt`
- `last.pt`

这证明：

- 模型结构是能训练的。
- 自定义模块接入没有语法性阻塞。

### 4.3 正在进行 50 epoch 正式训练

当前正式训练目录：

- `runs/weld_defect_research/formal_custom_yolo_50e/`

从前 12 个 epoch 的结果来看：

- mAP50 从 0.06103 提升到了 0.40569
- mAP50-95 从 0.01734 提升到了 0.19635

这说明模型在正常学习，不是空跑。

### 4.4 推理已经能输出“图片 + 文字报告”

当前已经完成了真正的单图检测链路：

- 输入焊缝图像
- 输出检测框结果图
- 输出文字分析报告
- 输出 JSON 报告

例如已经测试出的结果包括：

- 检出 Porosity
- 输出中高风险等级
- 给出文字建议

---

## 5. 这些代码怎么使用

下面按“最实用”的方式写。

### 5.1 只想直接识别一张焊缝图片

命令行方式：

    Set-Location "E:\desk_top\hanjie\weld_project"
    & "D:\miniconda\envs\machine\python.exe" scripts\predict_report.py --source 你的图片路径.jpg --device 0

输出内容：

- 标注结果图
- txt 报告
- json 报告

默认会：

- 优先找最新训练得到的 `best.pt`
- 如果找不到，则回退到 `yolo26n.pt`

### 5.2 不想敲命令，想点按钮识别

可以直接双击：

- `launch_gui.bat`

或者运行：

    Set-Location "E:\desk_top\hanjie\weld_project"
    & "D:\miniconda\envs\machine\python.exe" scripts\weld_gui.py

GUI 功能包括：

- 选择图片
- 选择整个图片文件夹做批量识别
- 选择权重
- 原图和结果图并排显示
- 查看文字分析
- 打开结果目录

如果你完全不想敲命令，可以直接这样用：

1. 双击 `launch_gui.bat`。
2. 做单张识别，就点“选择图片”。
3. 做批量识别，就点“选择文件夹”，选中一整个图片目录。
4. 权重路径如果已经自动出现，就不用改；如果没有，就手动选择 `runs/weld_defect_research/.../weights/best.pt`。
5. 点击“单张识别”或“批量识别”。
6. 识别完成后，到 `outputs/gui_predict` 看结果图；如果是批量识别，再到 `outputs/gui_predict/batch` 看 `batch_summary.csv`、`batch_summary.txt`、`batch_summary.json`。

### 5.3 想做正式训练

可以直接双击：

- `train_formal_50epochs.bat`
- `train_formal_100epochs.bat`

或者命令行运行：

    Set-Location "E:\desk_top\hanjie\weld_project"
    & "D:\miniconda\envs\machine\python.exe" scripts\train_custom_yolo.py --config configs\experiment.yaml --epochs 50 --batch 2 --imgsz 640 --workers 0 --device 0 --name formal_custom_yolo_50e

### 5.4 想先分析数据集

    Set-Location "E:\desk_top\hanjie\weld_project"
    & "D:\miniconda\envs\machine\python.exe" scripts\analyze_dataset.py --config configs\experiment.yaml

### 5.5 想确认“损失函数优化”真的进入训练了

运行：

   Set-Location "E:\desk_top\hanjie\weld_project"
   & "D:\miniconda\envs\machine\python.exe" scripts\verify_loss_chain.py --loss-type focal_eiou

如果屏幕输出里出现：

- `CustomV8DetectionLoss`
- `CustomBboxLoss`

就说明现在不是只改了网络结构，而是连训练时的损失计算也已经换成了我们自己的实现。

### 5.6 想把多次训练结果整理成论文里的表格

运行：

   Set-Location "E:\desk_top\hanjie\weld_project"
   & "D:\miniconda\envs\machine\python.exe" scripts\summarize_run.py runs\weld_defect_research\formal_custom_yolo_50e runs\weld_defect_research\loss_chain_verify_feiou_fast

这个脚本会直接给出每个训练目录的：

- 最佳 epoch
- 最佳 mAP50
- 最佳 mAP50-95
- precision
- recall

写论文实验部分时，这个脚本很有用。

---

## 6. 这个项目里最值得你学习的代码文件

如果你是训练小白，不要一上来就全部看。

建议按下面顺序学习。

### 第一层：先看“怎么用”

1. `README.md`
2. `scripts/predict_report.py`
3. `scripts/weld_gui.py`
4. `scripts/train_custom_yolo.py`

学习目标：

- 知道项目入口在哪。
- 知道训练和推理命令怎么写。
- 知道模型输出都保存在哪里。

### 第二层：再看“数据是怎么处理的”

1. `src/weld_project/data/dataset_utils.py`
2. `src/weld_project/data/transforms.py`

学习目标：

- 知道 polygon 标注怎么被转换成 bbox。
- 知道为什么推理前可以加传统图像预处理。

### 第三层：再看“模型优化模块”

1. `src/weld_project/models/modules/adaptive_preprocess.py`
2. `src/weld_project/models/modules/attention.py`
3. `src/weld_project/models/modules/enhanced_fpn.py`
4. `src/weld_project/models/ultralytics_modules.py`

学习目标：

- 了解每个模块解决了什么问题。
- 了解这些模块是怎么被组装进 YOLO 结构中的。

### 第四层：最后看“训练链路”和“损失函数”

1. `src/weld_project/integrations/ultralytics_custom.py`
2. `src/weld_project/integrations/ultralytics_loss.py`
3. `src/weld_project/models/modules/losses.py`
4. `configs/models/yolov8_weld_custom.yaml`

学习目标：

- 理解自定义结构怎么接到 Ultralytics。
- 理解自定义损失是怎么替换默认损失的。

---

## 7. 如果你自己学习，应该怎么调参

这一部分最重要。

不要一开始就“乱改一堆参数”。
要按顺序来。

### 7.1 第一阶段：先保证能稳定训练

优先看这几个参数：

- `epochs`
- `batch`
- `imgsz`
- `workers`
- `device`

建议：

1. 先固定 `workers=0`
   - 因为你当前是 Windows 环境，之前已经出现过 DataLoader worker 问题。

2. 先固定 `batch=2`
   - 你的显卡只有 4GB，盲目加 batch 容易爆显存。

3. 训练不稳定时，优先把 `imgsz` 从 640 降到 512
   - 速度会明显更快。
   - 代价是小目标精度可能略降。

### 7.2 第二阶段：先做一个“快实验”

如果你是自己学习，建议先做一轮小规模实验：

- `epochs=20`
- `imgsz=512`
- `batch=2`
- `workers=0`

原因：

- 能较快看到模型有没有学习趋势。
- 不至于一次等太久。

### 7.3 第三阶段：做标准实验

等你确认训练链路没问题，再做：

- `epochs=50`
- `imgsz=640`
- `batch=2`

这比较适合你当前设备。

### 7.4 第四阶段：做消融实验

建议你按这个顺序做：

1. baseline
2. preprocess
3. neck
4. attention
5. full

原因：

- 这样你能知道到底是哪一个模块真正带来了收益。
- 后面写论文、写报告时也更有说服力。

---

## 8. 后续如果你想继续优化，可以从哪里下手

### 8.1 先优化训练效率

可尝试：

- 把 `imgsz` 调成 512
- 训练前把数据缓存打开
- 试探性把 `batch` 调到 4（前提是不爆显存）

### 8.2 再优化检测精度

可尝试：

- 延长训练 epoch
- 提高图片尺寸
- 进一步改 FPN
- 把 EMA 和 SimAM 做对比
- 调整损失权重

### 8.3 再优化工程使用体验

可尝试：

- 批量图片识别
- 视频识别
- 生成 PDF 报告
- 增加识别历史记录
- 做一个更完整的桌面软件界面

---

## 9. 当前项目里哪些内容适合写进“创新点”或“工作内容”

如果你后面要写课程设计、毕设、论文中期报告，可以这样提炼。

### 可以写成“工作内容”的

- 完成焊缝缺陷数据集统计与标签格式适配。
- 构建基于 YOLOv8 的焊缝缺陷检测实验框架。
- 设计并实现自适应图像预处理模块。
- 设计并实现轻量化注意力模块。
- 设计并实现轻量化改进 FPN 模块。
- 将自定义损失函数接入检测训练链路。
- 实现单图检测、结果可视化和文字分析输出。

### 可以写成“创新点”的

- 将传统机器视觉预处理与深度检测模型结合。
- 针对焊缝小缺陷特征，设计轻量化多尺度增强结构。
- 在工业焊缝缺陷场景中，引入注意力机制抑制背景干扰。
- 将 Focal + EIoU 风格损失接入实际训练链路，用于应对类别不平衡与小目标定位困难。
- 构建面向实际使用的缺陷检测结果解释与报告输出机制。

---

## 10. 你现在最应该做什么

如果你是训练小白，我建议你不要再往外扩太多功能，先把下面 3 件事跑顺。

### 第一步

先等当前 50 epoch 训练完成。

你最需要看的文件是：

- `runs/weld_defect_research/formal_custom_yolo_50e/results.csv`
- `runs/weld_defect_research/formal_custom_yolo_50e/weights/best.pt`

### 第二步

训练完成后，用 `best.pt` 多测几张测试图，看看：

- 有没有明显漏检
- 有没有明显误检
- 哪一类最难识别

### 第三步

再决定下一轮优化方向：

- 如果主要问题是小缺陷漏检，优先继续优化 Neck 和图片尺寸。
- 如果主要问题是误检，优先优化注意力和阈值。
- 如果主要问题是类别不平衡，优先继续调损失函数。

---

## 11. 一句话总结

这个项目现在已经不是“只能跑个 YOLO baseline”的水平了。

它已经具备：

- 自定义结构训练
- 自定义损失接入
- 单图推理
- 图形界面
- 报告输出
- 继续做课题优化的基础框架

如果你后续继续学习，最重要的不是一次性加很多新模块，而是：

- 每次只改一个核心因素
- 看指标变化
- 保留实验记录
- 学会解释为什么这个改动有效

这才是研究和项目优化最关键的能力。
