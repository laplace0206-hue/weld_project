# 论文方法章节公式清单与写作模板

本文档面向本项目 weld_project，目的是直接为论文中的“方法章节”提供一份可落地的公式清单和写作模板。

适用前提：

- 基线模型采用 YOLOv8 检测框架。
- 改进点围绕自适应预处理、轻量注意力、改进特征融合和 Focal-EIoU 风格损失展开。
- 数据集类别包括 Crack、Porosity、Spatters、Welding line。

建议方法章节可以写成以下结构：

1. 总体框架
2. 自适应预处理模块
3. 轻量注意力与多尺度特征融合模块
4. 改进损失函数
5. 训练与推理流程

---

## 1. 方法章节应该保留哪些公式

对本项目来说，方法章节不需要堆太多公式，重点写 5 组即可：

1. 检测总体优化目标
2. 自适应预处理表达式
3. 改进特征融合表达式
4. 注意力加权表达式
5. Focal 分类损失与 EIoU 回归损失

如果篇幅有限，最少保留第 1、3、4、5 组即可。

---

## 2. 公式清单

## 2.1 总体优化目标

### 建议公式

$$
\mathcal{L}=\lambda_{box}\mathcal{L}_{box}+\lambda_{cls}\mathcal{L}_{cls}+\lambda_{dfl}\mathcal{L}_{dfl}
$$

### 符号说明

- $\mathcal{L}$ 表示模型总损失。
- $\mathcal{L}_{box}$ 表示边界框回归损失。
- $\mathcal{L}_{cls}$ 表示分类损失。
- $\mathcal{L}_{dfl}$ 表示分布式回归相关损失。
- $\lambda_{box}$、$\lambda_{cls}$、$\lambda_{dfl}$ 表示对应损失项的权重。

### 写作模板

可直接写成：

本文以 YOLOv8 检测框架为基础，整体优化目标由边界框回归损失、分类损失以及分布式定位损失构成，其表达式如式(1)所示。其中，$\mathcal{L}_{box}$ 用于约束预测框与真实框之间的几何差异，$\mathcal{L}_{cls}$ 用于提升缺陷类别判别能力，$\mathcal{L}_{dfl}$ 用于增强边界回归的细粒度表达能力。通过对三类损失进行联合优化，可以同时兼顾焊缝缺陷目标的定位精度与类别识别性能。

---

## 2.2 自适应预处理模块

### 建议公式

如果你希望方法章节里显式体现传统机器视觉思想，可以用下面这个统一表达：

$$
I_{enh}=\phi(I)=\alpha \cdot \text{CLAHE}(I)+\beta \cdot \text{Edge}(I)+\gamma \cdot I
$$

其中约束为：

$$
\alpha+\beta+\gamma=1
$$

### 符号说明

- $I$ 表示输入焊缝图像。
- $I_{enh}$ 表示增强后的图像。
- $\text{CLAHE}(I)$ 表示对比度受限自适应直方图均衡结果。
- $\text{Edge}(I)$ 表示边缘增强结果。
- $\alpha$、$\beta$、$\gamma$ 表示不同分量的融合权重。

### 写作模板

可直接写成：

考虑到焊缝图像中常存在光照不均、表面反光和局部低对比度等问题，本文在输入端引入自适应预处理模块。该模块通过对比度增强、边缘增强以及原始图像信息的加权融合，生成更适合后续特征提取的输入表达，如式(2)所示。通过该设计，可以在保留原始纹理结构的同时增强裂纹、气孔和飞溅等细微缺陷区域的可分辨性。

### 可选补充句

如果导师更偏向“传统机器视觉 + 深度学习结合”的表述，可以补一句：

该模块并非替代深度特征学习，而是利用传统图像增强手段在前端提升缺陷先验显著性，从而减轻主干网络对低对比度缺陷的学习压力。

---

## 2.3 改进多尺度特征融合

### 建议公式

针对 BiFPN 或轻量特征融合模块，建议采用加权融合表达式：

$$
F_{out}=\frac{\sum_{i=1}^{N} w_i F_i}{\sum_{i=1}^{N} w_i+\varepsilon}
$$

其中：

$$
w_i \ge 0
$$

### 符号说明

- $F_i$ 表示第 $i$ 个尺度的输入特征图。
- $F_{out}$ 表示融合后的输出特征图。
- $w_i$ 表示第 $i$ 个尺度特征的可学习权重。
- $\varepsilon$ 为防止分母为 0 的极小常数。

### 写作模板

可直接写成：

为提升焊缝缺陷中小目标与细长目标的检测能力，本文在颈部网络中采用轻量化加权特征融合策略，对不同尺度特征进行自适应整合，其表达式如式(3)所示。与传统 FPN 中固定相加的方式相比，该方法能够根据训练过程自动调整不同层特征的贡献度，使浅层纹理细节与深层语义信息实现更加合理的协同，从而增强对裂纹、气孔等尺度较小缺陷的表征能力。

### 可选补充句

如果需要强调本项目中的 LiteFPNRefine，可以追加一句：

在具体实现上，本文进一步结合轻量卷积与残差式细化结构，对融合后的特征进行再校正，以减小多尺度信息直接叠加带来的语义冲突问题。

---

## 2.4 注意力增强模块

### 建议公式

对于注意力模块，用简洁表达即可，不建议写得过于复杂：

$$
F^{'}=A(F)\odot F + F
$$

若需要拆成通道与空间两个部分，可写为：

$$
A_c=\sigma(g_c(F)), \quad A_s=\sigma(g_s(F))
$$

$$
F^{'}=A_s \odot (A_c \odot F)+F
$$

### 符号说明

- $F$ 表示输入特征图。
- $A(F)$ 表示由输入特征计算得到的注意力权重。
- $A_c$ 表示通道注意力。
- $A_s$ 表示空间注意力。
- $\sigma(\cdot)$ 表示 Sigmoid 激活函数。
- $\odot$ 表示逐元素相乘。

### 写作模板

可直接写成：

由于焊缝表面纹理复杂，且背景中常包含与缺陷区域相似的高亮边缘和噪声干扰，仅依赖常规卷积特征容易产生误检。为此，本文在特征增强阶段引入轻量注意力机制，通过对关键通道和关键空间区域进行加权，抑制无关背景响应并突出缺陷相关区域，其表达式如式(4)和式(5)所示。该设计能够提高网络对微小裂纹、局部气孔和飞溅聚集区域的聚焦能力。

### 可选补充句

如果你想和代码中的 WeldEMA 对齐，可以写成：

本文采用的轻量注意力模块在保持较低参数量与计算量的同时，增强了模型对局部异常纹理的响应能力，适合资源受限条件下的工业视觉检测任务。

---

## 2.5 Focal 分类损失

### 建议公式

$$
\mathcal{L}_{cls}=-\alpha (1-p_t)^{\gamma}\log(p_t)
$$

### 符号说明

- $p_t$ 表示模型对真实类别的预测概率。
- $\alpha$ 表示类别平衡因子。
- $\gamma$ 表示难易样本调制因子。

### 写作模板

可直接写成：

焊缝缺陷检测任务中存在明显的前景背景不平衡和类别不平衡问题，尤其是裂纹样本数量相对有限，而背景与易分类样本占比较高。为减弱易分类样本在训练过程中的主导作用，本文在分类分支中采用 Focal Loss，其表达式如式(6)所示。该损失通过引入调制因子 $(1-p_t)^{\gamma}$，降低高置信度样本对总体梯度的影响，从而促使网络更加关注难分类缺陷样本。

---

## 2.6 EIoU 风格边界框回归损失

### 建议公式

$$
\mathcal{L}_{EIoU}=1-IoU+\frac{\rho^2(b,b^{gt})}{c^2}+\frac{(w-w^{gt})^2}{c_w^2}+\frac{(h-h^{gt})^2}{c_h^2}
$$

### 符号说明

- $IoU$ 表示预测框与真实框的交并比。
- $\rho^2(b,b^{gt})$ 表示预测框中心点与真实框中心点之间的欧式距离平方。
- $c$ 表示最小外接框对角线长度。
- $w$、$h$ 表示预测框宽和高。
- $w^{gt}$、$h^{gt}$ 表示真实框宽和高。
- $c_w$、$c_h$ 分别表示外接框宽度和高度。

### 写作模板

可直接写成：

针对焊缝缺陷目标尺度小、形态细长且边界不规则的特点，本文在边界框回归分支中采用 EIoU 风格损失，其表达式如式(7)所示。与仅关注重叠面积的 IoU 类损失相比，EIoU 在考虑中心点偏移的同时，进一步显式约束预测框与真实框在宽度和高度上的差异，因此更适合描述焊缝裂纹、狭长焊缝线等形态敏感目标的定位误差。

---

## 2.7 Focal-EIoU 联合损失

### 建议公式

如果你希望在论文里明确把项目的损失创新写成“联合优化”，建议写为：

$$
\mathcal{L}_{box}^{*}=\omega \cdot \mathcal{L}_{EIoU}
$$

其中 $\omega$ 可以写成与目标质量或样本难度相关的权重。若想表达得更完整，可写为：

$$
\mathcal{L}_{custom}=\lambda_{1}\mathcal{L}_{Focal}+\lambda_{2}\mathcal{L}_{EIoU}+\lambda_{3}\mathcal{L}_{DFL}
$$

### 写作模板

可直接写成：

为了同时缓解类别不平衡问题与定位精度不足问题，本文将 Focal 风格分类损失与 EIoU 风格回归损失进行联合建模，构建适用于焊缝缺陷检测的改进损失函数。该策略在分类层面强化了网络对困难样本的关注能力，在定位层面增强了对目标框中心偏移和宽高误差的约束能力，从而提高整体检测性能与训练稳定性。

如果你要突出“真正接入训练链路”，可以加一句：

需要说明的是，本文并非仅在理论层面设计损失函数，而是将该损失以自定义 criterion 的形式嵌入实际检测训练流程中，实现了从模型构建到参数优化的端到端一致性。

---

## 3. 方法章节整体写作模板

下面给你一份可以直接改字使用的“方法章节模板”。

## 3.1 方法总述模板

可直接写成：

针对工业焊缝缺陷检测中存在的目标尺度小、背景干扰强、类别分布不均衡以及定位精度不足等问题，本文提出一种基于 YOLOv8 改进的焊缝缺陷检测方法。该方法在保留单阶段检测框架高效性的基础上，从输入端预处理、特征融合、注意力增强以及损失函数设计四个方面进行优化。首先，在输入端引入自适应预处理模块，以增强低对比度缺陷区域的纹理与边缘响应；其次，在颈部网络中采用轻量化多尺度特征融合策略，以提升对微小缺陷和细长结构的表达能力；然后，通过轻量注意力机制抑制复杂背景噪声，增强网络对缺陷关键区域的聚焦能力；最后，引入 Focal-EIoU 风格联合损失，以提升类别不平衡场景下的分类鲁棒性和边界定位精度。整体框架在保证检测效率的同时，增强了模型对焊缝裂纹、气孔、飞溅和焊缝线等目标的综合检测能力。

---

## 3.2 自适应预处理小节模板

可直接写成：

焊缝图像通常受到成像光照、金属表面反光以及噪声干扰等因素影响，导致部分缺陷区域表现为低对比度、弱纹理甚至边界模糊。若直接将原始图像输入检测网络，容易降低模型对细微缺陷的感知能力。为此，本文设计自适应预处理模块，对原始输入进行对比度增强和边缘信息强化，其数学表达如式(2)所示。通过对增强结果与原始图像进行融合，可以在尽量保留原始结构信息的前提下突出潜在缺陷区域，提高后续主干网络的特征提取质量。

---

## 3.3 特征融合小节模板

可直接写成：

焊缝缺陷目标往往具有尺度变化大、局部细节弱和形态不规则等特点，传统单一路径的特征传播方式难以兼顾浅层纹理信息与深层语义信息。针对该问题，本文在特征金字塔结构中引入轻量化加权融合策略，对不同尺度特征进行自适应整合，其表达式如式(3)所示。与固定权重融合方式相比，该策略能够根据训练过程动态分配不同尺度特征的重要性，使模型更有效地捕获微小缺陷与复杂结构缺陷的多尺度表征信息。

---

## 3.4 注意力增强小节模板

可直接写成：

由于焊缝表面纹理、焊接飞溅和高亮边缘等背景信息容易与真实缺陷产生混淆，单纯依靠卷积操作难以稳定地区分目标区域与干扰区域。为此，本文引入轻量注意力增强模块，对通道维度和空间维度的特征响应进行联合建模，其表达式如式(4)和式(5)所示。该模块能够提升缺陷相关区域的响应强度，同时抑制无关背景信息，从而有效降低误检率并提升模型对细粒度缺陷的识别能力。

---

## 3.5 损失函数小节模板

可直接写成：

在焊缝缺陷检测任务中，一方面不同类别样本数量存在较大差异，另一方面小目标和细长目标的边界回归难度较高，导致传统损失函数在训练过程中难以同时兼顾分类鲁棒性和定位准确性。为此，本文采用 Focal 风格分类损失与 EIoU 风格回归损失进行联合优化。分类损失通过调节易分类样本的梯度贡献，促使模型更加关注困难缺陷样本；回归损失则从中心偏移、宽度偏差和高度偏差三个方面对预测框进行约束，从而提升对不规则焊缝缺陷目标的定位能力。改进后的联合损失函数如式(6)至式(8)所示。

---

## 3.6 方法章节结尾模板

可直接写成：

综上，本文提出的焊缝缺陷检测方法以 YOLOv8 为基础，在输入增强、特征融合、注意力建模和损失优化等方面进行了针对性改进。上述模块协同作用，使模型能够更好地适应焊缝缺陷场景中小目标多、背景复杂和类别不平衡等问题，为后续实验验证和工程应用提供了方法基础。

---

## 4. 论文写作时的落地建议

## 4.1 公式数量控制建议

建议最终保留 6 到 8 个公式即可，不要把代码里的每个细节都翻成公式。

最推荐保留的是：

1. 总损失公式
2. 自适应预处理公式
3. 多尺度加权融合公式
4. 注意力增强公式
5. Focal Loss
6. EIoU Loss
7. 联合损失公式

## 4.2 和项目代码的对应关系

论文中可对应到本项目的如下实现：

- 自适应预处理：输入增强与传统机器视觉预处理逻辑
- 轻量注意力：WeldEMA 对应的注意力增强思想
- 特征融合：LiteFPNRefine 对应的轻量多尺度融合思想
- 损失优化：focal_eiou 对应的 CustomV8DetectionLoss 和 CustomBboxLoss

## 4.3 写作时不要犯的错误

1. 不要只写“本文改进了 YOLOv8”，必须逐点说明改进解决了什么问题。
2. 不要只给公式，不解释每个公式为什么适合焊缝缺陷场景。
3. 不要把模块写成互相割裂的几个点，要强调它们是协同优化。
4. 不要把工程实现细节全部塞进方法章节，代码级细节放实现部分或实验设置部分。

---

## 5. 最简可直接套用版

如果你现在只想先写出一个能交差、还能看起来比较完整的方法章节，可以直接按下面顺序写：

1. 第一段：说明焊缝缺陷检测的难点。
2. 第二段：说明本文从预处理、特征融合、注意力和损失函数四方面改进 YOLOv8。
3. 第三段：给出总损失公式。
4. 第四段：给出预处理公式并解释。
5. 第五段：给出特征融合公式并解释。
6. 第六段：给出注意力公式并解释。
7. 第七段：给出 Focal Loss 和 EIoU Loss，并说明为什么组合。
8. 第八段：做一个总括。

这样写，基本就能构成一节完整的方法章节。

---

## 6. 后续可继续补充的内容

后面如果你准备继续写论文，还可以在这个文档基础上继续补：

- 方法章节配图清单
- 实验章节表格模板
- 创新点摘要模板
- 摘要和结论模板
